# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.tox/

# Local config / secrets
.env
.env.local
*.env.backup

# Benchmark output
results/
results.bak-*/
*.bak
*.cache/

# Internal audit / scratch — never commit
.audit-*/
scripts/audit_cost_probe.py

# Run logs - do NOT keep these in the repo. They are useful historical context,
# so archive them OUTSIDE the source tree (e.g.
# ../analysis/khora-graphrag-bench-runlogs/) rather than committing or leaving
# them here.
*.log

# Coverage artifacts (CI)
coverage.xml
coverage.json

# Local agent / session tooling
.claude/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*.swo
